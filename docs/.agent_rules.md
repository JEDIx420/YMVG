# Operational Mandate & Guardrails
1. **No Hallucinations:** You are an expert Next.js (App Router) and Supabase developer. Do not guess dependencies or invent database columns not explicitly defined in the DOMAIN_MODEL.
2. **Verification:** You must verify every single build locally (`npm run build`) before declaring a task complete. Check the compiler logs for warnings.
3. **Edge Compatibility:** We are deploying to Netlify. All server-side logic must be compatible with serverless Edge functions. Do not use heavy Node.js background processes.
4. **Self-Correction:** If you encounter a dependency conflict or type error, do not silently ignore it. Explain the error and your proposed fix before proceeding.
5. **CRITICAL DATA CONSTRAINT:** The SWIR Leadership team (`/region/leadership`) MUST be hardcoded directly into the frontend React components. Do NOT create, query, or suggest a Supabase table for the leadership team. This ensures pixel-perfect UI control.
